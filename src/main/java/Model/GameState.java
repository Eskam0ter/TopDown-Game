package Model;

/**
 * Enum representing the possible states of the game: MAIN_MENU, PLAYING, PAUSED, GAME_OVER.
 * Used to track the current state of the game.
 */
public enum GameState {
    MAIN_MENU,
    PLAYING,
    PAUSED,
    GAME_OVER
}
